# --- secrets (never commit) ---
.env
*.env
!.env.example
openrouter-key.env

# --- runtime / logs ---
*.log
logs/
decisions*.jsonl

# --- deps / build ---
node_modules/
dist/
.devbox/
.direnv/

# --- os ---
.DS_Store

# SDD scratch (ledger)
.superpowers/

# ESLint cache
.eslintcache

# Claude Code: active routing config is opt-in (copy from .example); local is personal
.claude/settings.json
.claude/settings.local.json
